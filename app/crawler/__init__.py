"""模块1：全网资产采集层。

设计取向（重要，决定了后续怎么扩展）：

    适配器负责"把某个平台的页面变成标准字典"，其余全部下沉为公共能力：
      归一化 → 文本结构化 → 指纹去重 → 入库 → 自动评分

一期交付的是**可用的骨架 + 完整的数据管道**：去重、结构化、入库、评分、
调度入口全部真实可用；每个平台的 `fetch_live()` 需要补上真实 DOM 选择器
并在确认授权后打开开关才能出网采集。

这样做的好处：换一个平台 = 新增一个 adapter 文件，公共管道一行不改。
"""
